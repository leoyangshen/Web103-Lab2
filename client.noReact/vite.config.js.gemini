// vite.config.js

import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react'; // if you're using react

export default defineConfig({
  plugins: [react()], // or an empty array if not using react
  server: {
    proxy: {
      '/gifts': 'http://localhost:3001',
    },
  },
});
