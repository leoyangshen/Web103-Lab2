import { defineConfig } from 'vite'

export default defineConfig({
  build: {
    outDir: '../server/public',
    emptyOutDir: true
  },
  server: {
    proxy: {
      '/gifts': {
        target: 'http://localhost:3001',
	changeOrigin: true,
// Optional: rewrite the path if necessary (e.g., if Express expects '/api/gifts')
// rewrite: (path) => path.replace(/^\/gifts/, '/api/gifts')
      }
    }
  },
	publicDir: "../server/public"
})
